<style is:global>
  :root {
    --cs-accent: #1a1a1a;
    --cs-accent-light: #f5f4f0;
    --cs-tag-bg: #efefec;
    --cs-tag-color: #444;
    --cs-border: #e0dfd9;
    --cs-muted: #777;
    --cs-good: #1a7a4a;
    --cs-good-bg: #edfaf3;
    --cs-bad: #a0291a;
    --cs-bad-bg: #fdf0ee;
  }

  .cs-hero,
  .cs-section {
    max-width: 720px;
    margin: 0 auto;
    padding: 4rem 1.5rem;
  }

  .cs-figure {
    max-width: 900px;
    margin: 2.5rem auto;
    padding: 0 1.5rem;
  }
  .cs-figure img {
    width: 100%;
    border-radius: 8px;
    border: 1px solid var(--cs-border);
  }
  .cs-figure figcaption {
    margin-top: 0.6rem;
    font-size: 0.8rem;
    color: var(--cs-muted);
    text-align: center;
  }
  .cs-figure--full {
    width: 100vw;
    max-width: 100vw;
    margin-left: calc(50% - 50vw);
    padding: 0;
  }
  .cs-figure--full img { border-radius: 0; border: none; box-shadow: none; background: none; }

  .cs-hero__meta {
    display: flex;
    gap: 0.5rem;
    margin-bottom: 1.5rem;
    flex-wrap: wrap;
  }
  .cs-tag {
    font-size: 0.75rem;
    padding: 0.3rem 0.75rem;
    background: var(--cs-tag-bg);
    color: var(--cs-tag-color);
    border-radius: 999px;
    font-weight: 500;
    letter-spacing: 0.02em;
  }
  .cs-hero__title {
    font-size: clamp(2.2rem, 6vw, 3.5rem);
    line-height: 1.1;
    margin-bottom: 1.25rem;
    font-weight: 700;
  }
  .cs-hero__title em {
    font-style: normal;
    opacity: 0.5;
  }
  .cs-hero__subtitle {
    font-size: 1.15rem;
    line-height: 1.65;
    color: #444;
    margin-bottom: 2.5rem;
    max-width: 60ch;
  }
  .cs-hero__specs {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
    gap: 1.5rem;
    padding-top: 2rem;
    border-top: 1px solid var(--cs-border);
  }
  .cs-spec__label {
    display: block;
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--cs-muted);
    margin-bottom: 0.3rem;
    font-weight: 600;
  }
  .cs-spec__value { font-size: 0.9rem; line-height: 1.5; }

  .cs-section__label {
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--cs-muted);
    font-weight: 600;
    margin-bottom: 0.75rem;
  }
  .cs-section__title {
    font-size: clamp(1.5rem, 3.5vw, 2rem);
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: 1.75rem;
  }
  .cs-section--closing {
    border-top: 1px solid var(--cs-border);
    padding-top: 5rem;
  }

  .cs-prose p {
    font-size: 1.05rem;
    line-height: 1.75;
    color: #333;
    margin-bottom: 1.25rem;
    max-width: 65ch;
  }
  .cs-prose p:last-child { margin-bottom: 0; }
  .cs-prose--mt { margin-top: 2rem; }
  .cs-prose--large p { font-size: 1.15rem; line-height: 1.8; }

  .cs-findings {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 1rem;
    margin-top: 2rem;
  }
  .cs-finding {
    background: var(--cs-accent-light);
    border-left: 3px solid #ccc;
    padding: 1.25rem 1.25rem 1rem;
    margin: 0;
    border-radius: 0 6px 6px 0;
  }
  .cs-finding p {
    font-size: 0.95rem;
    line-height: 1.55;
    margin-bottom: 0.6rem;
    color: #222;
  }
  .cs-finding cite {
    font-size: 0.75rem;
    color: var(--cs-muted);
    font-style: normal;
  }

  .cs-highlight-block {
    background: var(--cs-accent);
    color: #fff;
    padding: 2.5rem;
    border-radius: 10px;
    margin: 2.5rem 0;
  }
  .cs-highlight-block__question {
    font-size: clamp(1.3rem, 3vw, 1.8rem);
    font-weight: 700;
    font-style: italic;
    line-height: 1.3;
    margin-bottom: 0.75rem;
  }
  .cs-highlight-block__desc {
    font-size: 1rem;
    opacity: 0.75;
    line-height: 1.6;
  }

  .cs-principles {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 1.5rem;
    margin-top: 2rem;
  }
  .cs-principle { border-top: 2px solid var(--cs-accent); padding-top: 1rem; }
  .cs-principle__num {
    font-size: 0.75rem;
    color: var(--cs-muted);
    font-weight: 600;
    letter-spacing: 0.1em;
    display: block;
    margin-bottom: 0.4rem;
  }
  .cs-principle__title { font-size: 1.1rem; font-weight: 700; margin-bottom: 0.5rem; }
  .cs-principle__desc { font-size: 0.9rem; color: #555; line-height: 1.6; }

  .cs-layers { margin-top: 2rem; display: flex; flex-direction: column; gap: 0; }
  .cs-layer {
    display: flex;
    align-items: flex-start;
    gap: 1.25rem;
    padding: 1.25rem;
    border: 1px solid var(--cs-border);
    border-radius: 8px;
    margin-bottom: 0.5rem;
    background: #fafaf8;
  }
  .cs-layer--connector {
    background: transparent;
    border: none;
    padding: 0 1.25rem;
    font-size: 1.2rem;
    color: var(--cs-muted);
    align-items: center;
  }
  .cs-layer__badge {
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-weight: 700;
    background: var(--cs-accent);
    color: #fff;
    padding: 0.2rem 0.6rem;
    border-radius: 4px;
    white-space: nowrap;
    margin-top: 0.15rem;
  }
  .cs-layer__content h4 { font-weight: 700; margin-bottom: 0.25rem; font-size: 1rem; }
  .cs-layer__content p { font-size: 0.88rem; color: #555; line-height: 1.55; margin: 0; }

  .cs-token-levels { margin-top: 2rem; }
  .cs-token-level { border-top: 1px solid var(--cs-border); padding: 1.75rem 0; }
  .cs-token-level__header { display: flex; align-items: baseline; gap: 0.75rem; margin-bottom: 0.75rem; }
  .cs-token-level__num { font-size: 0.75rem; color: var(--cs-muted); font-weight: 600; }
  .cs-token-level h3 { font-size: 1.1rem; font-weight: 700; }
  .cs-token-level p { font-size: 0.95rem; color: #444; line-height: 1.65; max-width: 60ch; margin-bottom: 1rem; }
  .cs-token-example { display: flex; gap: 0.5rem; flex-wrap: wrap; }
  .cs-token-example code {
    font-family: 'SF Mono', 'Fira Code', monospace;
    font-size: 0.78rem;
    background: var(--cs-tag-bg);
    padding: 0.25rem 0.6rem;
    border-radius: 4px;
    color: #333;
  }

  .cs-component-pillars {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 1.5rem;
    margin: 2rem 0;
  }
  .cs-pillar { border: 1px solid var(--cs-border); border-radius: 8px; padding: 1.5rem; }
  .cs-pillar__title { font-size: 1rem; font-weight: 700; margin-bottom: 0.5rem; }
  .cs-pillar__desc { font-size: 0.88rem; color: #555; line-height: 1.6; }

  .cs-writing-patterns { display: flex; flex-direction: column; gap: 2rem; margin-top: 2rem; }
  .cs-pattern { border: 1px solid var(--cs-border); border-radius: 8px; overflow: hidden; }
  .cs-pattern__title {
    font-size: 0.85rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    padding: 1rem 1.25rem 0.75rem;
    border-bottom: 1px solid var(--cs-border);
    background: #fafaf8;
  }
  .cs-pattern__comparison { display: grid; grid-template-columns: 1fr 1fr; }
  @media (max-width: 520px) { .cs-pattern__comparison { grid-template-columns: 1fr; } }
  .cs-pattern__bad, .cs-pattern__good { padding: 1.25rem; }
  .cs-pattern__bad { background: var(--cs-bad-bg); border-right: 1px solid var(--cs-border); }
  .cs-pattern__good { background: var(--cs-good-bg); }
  .cs-pattern__badge {
    display: inline-block;
    font-size: 0.65rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    padding: 0.15rem 0.5rem;
    border-radius: 3px;
    margin-bottom: 0.6rem;
  }
  .cs-pattern__badge--bad { background: var(--cs-bad); color: #fff; }
  .cs-pattern__badge--good { background: var(--cs-good); color: #fff; }
  .cs-pattern__bad p, .cs-pattern__good p { font-size: 0.95rem; line-height: 1.5; margin: 0; }
  .cs-pattern__reason {
    font-size: 0.82rem;
    color: var(--cs-muted);
    padding: 0.75rem 1.25rem;
    border-top: 1px solid var(--cs-border);
    line-height: 1.55;
    margin: 0;
    background: #fafaf8;
  }

  .cs-callout {
    border-left: 4px solid var(--cs-accent);
    padding: 1.25rem 1.5rem;
    background: var(--cs-accent-light);
    border-radius: 0 8px 8px 0;
    margin: 2rem 0;
  }
  .cs-callout p { font-size: 0.95rem; line-height: 1.65; margin: 0; color: #222; }

  .cs-timeline {
    position: relative;
    margin-top: 2rem;
    padding-left: 2rem;
    border-left: 2px solid var(--cs-border);
  }
  .cs-timeline__item { position: relative; padding-bottom: 2rem; }
  .cs-timeline__item:last-child { padding-bottom: 0; }
  .cs-timeline__marker {
    position: absolute;
    left: calc(-2rem - 5px);
    top: 0.35rem;
    width: 10px;
    height: 10px;
    background: var(--cs-accent);
    border-radius: 50%;
  }
  .cs-timeline__content h4 { font-size: 1rem; font-weight: 700; margin-bottom: 0.35rem; }
  .cs-timeline__content p { font-size: 0.9rem; color: #555; line-height: 1.6; margin: 0; }

  .cs-outcomes {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
    gap: 1.5rem;
    margin-top: 2.5rem;
  }
  .cs-outcome { padding: 1.5rem; border: 1px solid var(--cs-border); border-radius: 8px; background: #fafaf8; }
  .cs-outcome h4 { font-size: 0.95rem; font-weight: 700; margin-bottom: 0.4rem; }
  .cs-outcome p { font-size: 0.85rem; color: #555; line-height: 1.6; margin: 0; }

  .cs-figure--mt { margin-top: 2rem; }

  .cs-subsection-title {
    font-size: 1.1rem;
    font-weight: 700;
    margin: 2.5rem 0 1.25rem;
  }

  .cs-layer__badge--1 { background: #fdf0ee; color: #a0291a; }
  .cs-layer__badge--2 { background: #fef9ec; color: #8a6000; }
  .cs-layer__badge--3 { background: #edf7ff; color: #1a5a8a; }
  .cs-layer__badge--4 { background: #f0f0fe; color: #4a4a9a; }
  .cs-layer__badge--5 { background: var(--cs-accent-light); color: var(--cs-muted); }

  .cs-outcome__icon { font-size: 1.4rem; display: block; margin-bottom: 0.75rem; }

  .cs-arch-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 1rem;
    margin: 1.5rem auto;
    max-width: 900px;
    padding: 0 1.5rem;
  }
  .cs-arch-item { margin: 0; }
  .cs-arch-item img {
    width: 100%;
    border-radius: 6px;
    border: 1px solid var(--cs-border);
    display: block;
  }

  .marquee-wrapper {
    width: calc(100% + 300px);
    margin: 2rem -150px;
    overflow: hidden;
    border-radius: 12px;
  }
  .marquee-track {
    display: flex;
    gap: 1.5rem;
    width: max-content;
    animation: marquee-scroll 30s linear infinite;
  }
  .marquee-wrapper:hover .marquee-track {
    animation-play-state: paused;
  }
  @keyframes marquee-scroll {
    0%   { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }
  .marquee-slide {
    flex-shrink: 0;
    width: clamp(434px, 50vw, 686px);
  }
  .marquee-slide img {
    width: 100%;
    border-radius: 12px;
    border: 1px solid var(--cs-border);
    box-shadow: 0 4px 20px rgba(0,0,0,0.08);
    display: block;
    background: #fff;
  }

  .cs-comparison {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
    margin: 1.5rem 0;
  }
  @media (max-width: 600px) { .cs-comparison { grid-template-columns: 1fr; } }
  .cs-comparison__item figure { margin: 0 0 0.75rem; }
  .cs-comparison__item img {
    width: 100%;
    border-radius: 8px;
    border: 1px solid var(--cs-border);
    display: block;
  }
  .cs-comparison__label {
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-weight: 700;
    padding: 0.25rem 0.75rem;
    border-radius: 4px;
    display: inline-block;
    margin-bottom: 0.75rem;
  }
  .cs-comparison__label--before { background: var(--cs-bad-bg); color: var(--cs-bad); }
  .cs-comparison__label--after { background: var(--cs-good-bg); color: var(--cs-good); }
  .cs-comparison__desc { font-size: 0.85rem; line-height: 1.6; color: #555; }

  .cs-methods {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
    margin-top: 1.75rem;
  }
  @media (max-width: 560px) { .cs-methods { grid-template-columns: 1fr; } }
  .cs-method { border: 1px solid var(--cs-border); border-radius: 8px; padding: 1.5rem; background: #fafaf8; }
  .cs-method__title { font-size: 0.95rem; font-weight: 700; margin-bottom: 0.6rem; }
  .cs-method__desc { font-size: 0.88rem; color: #555; line-height: 1.65; }

  @media (prefers-color-scheme: dark) {
    :root {
      --cs-accent: #f0efe9;
      --cs-accent-light: #1e1e1c;
      --cs-tag-bg: #2a2a28;
      --cs-tag-color: #bbb;
      --cs-border: #2e2e2c;
      --cs-muted: #888;
      --cs-good-bg: #0f2a1c;
      --cs-bad-bg: #2a1010;
    }
    .cs-hero__subtitle, .cs-prose p { color: #bbb; }
    .cs-highlight-block { background: #f0efe9; color: #111; }
    .cs-layer, .cs-pillar, .cs-outcome, .cs-method { background: #1a1a18; }
    .cs-pattern__title, .cs-pattern__reason, .cs-callout, .cs-finding { background: #1a1a18; }
    .cs-layer__badge { background: #f0efe9; color: #111; }
    .cs-layer__badge--5 { background: #2a2a28; }
    .cs-token-example code { background: #2a2a28; color: #ddd; }
    .cs-comparison__desc, .cs-method__desc, .cs-layer__content p { color: #888; }
  }

  /* ===================== COMPRA ÁGIL ===================== */

  :root {
    --ca-blue: #0062C3;
    --ca-green: #0F6E56;
    --ca-green-bg: #E1F5EE;
    --ca-blue-bg: #E6F1FB;
    --ca-purple-bg: #EEEDFE;
    --ca-purple: #3C3489;
    --ca-amber: #633806;
    --ca-amber-bg: #FAEEDA;
    --ca-red: #791F1F;
    --ca-red-bg: #FCEBEB;
    --ca-gray: #444441;
    --ca-gray-bg: #F1EFE8;
    --ca-border-ca: #e0dfd9;
    --ca-muted: #777;
    --ca-surface: #fafaf8;
  }

  .ca-hero, .ca-section {
    max-width: 720px;
    margin: 0 auto;
    padding: 4rem 1.5rem;
  }

  .ca-fig {
    max-width: 900px;
    margin: 2rem auto;
    padding: 0 1.5rem;
  }
  .ca-fig img {
    width: 100%;
    border-radius: 8px;
    border: 1px solid var(--ca-border-ca);
    display: block;
  }
  .ca-fig figcaption {
    margin-top: 0.6rem;
    font-size: 0.78rem;
    color: var(--ca-muted);
    text-align: center;
  }
  .ca-fig--full {
    width: 100vw;
    max-width: 100vw;
    margin-left: calc(50% - 50vw);
    padding: 0;
  }
  .ca-fig--full img { border-radius: 0; border: none; box-shadow: none; background: none; }
  .ca-fig--inline { max-width: 720px; }
  .ca-fig--screen img { border-radius: 6px; }

  .ca-tags { display: flex; flex-wrap: wrap; gap: 6px; margin-bottom: 1.25rem; }
  .ca-tag {
    font-size: 0.72rem;
    padding: 3px 10px;
    background: var(--ca-gray-bg);
    color: var(--ca-gray);
    border-radius: 999px;
    font-weight: 500;
    letter-spacing: 0.02em;
  }
  .ca-hero__title {
    font-size: clamp(2.2rem, 6vw, 3.5rem);
    font-weight: 700;
    line-height: 1.1;
    margin-bottom: 1.1rem;
  }
  .ca-hero__title em { font-style: normal; opacity: 0.4; }
  .ca-hero__sub {
    font-size: 1.1rem;
    line-height: 1.75;
    color: #444;
    max-width: 60ch;
    margin-bottom: 2.5rem;
  }
  .ca-specs {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
    gap: 1.5rem;
    padding-top: 2rem;
    border-top: 1px solid var(--ca-border-ca);
  }
  .ca-spec__label {
    display: block;
    font-size: 0.68rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--ca-muted);
    font-weight: 600;
    margin-bottom: 3px;
  }
  .ca-spec__val { font-size: 0.9rem; line-height: 1.5; }

  .ca-metrics {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
    gap: 1rem;
  }
  .ca-metric {
    background: var(--ca-surface);
    border: 1px solid var(--ca-border-ca);
    border-radius: 8px;
    padding: 1.25rem;
  }
  .ca-metric__num { display: block; font-size: 1.6rem; font-weight: 700; line-height: 1; margin-bottom: 0.4rem; }
  .ca-metric__lbl { font-size: 0.8rem; color: var(--ca-muted); line-height: 1.5; }

  .ca-section__label {
    font-size: 0.68rem;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--ca-muted);
    font-weight: 600;
    margin-bottom: 0.65rem;
  }
  .ca-section__title {
    font-size: clamp(1.5rem, 3.5vw, 2rem);
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: 1.75rem;
  }
  .ca-section--closing { border-top: 1px solid var(--ca-border-ca); padding-top: 5rem; }

  .ca-prose p {
    font-size: 1.05rem;
    line-height: 1.8;
    color: #333;
    margin-bottom: 1.25rem;
    max-width: 65ch;
  }
  .ca-prose p:last-child { margin-bottom: 0; }
  .ca-prose--lg p { font-size: 1.1rem; }

  .ca-findings { display: flex; flex-direction: column; gap: 1rem; }
  .ca-finding {
    border: 1px solid var(--ca-border-ca);
    border-radius: 8px;
    padding: 1.25rem 1.5rem;
    background: var(--ca-surface);
  }
  .ca-finding__badge {
    display: inline-block;
    font-size: 0.7rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    padding: 2px 8px;
    border-radius: 4px;
    margin-bottom: 0.6rem;
  }
  .ca-badge--red { background: var(--ca-red-bg); color: var(--ca-red); }
  .ca-badge--amber { background: var(--ca-amber-bg); color: var(--ca-amber); }
  .ca-badge--gray { background: var(--ca-gray-bg); color: var(--ca-gray); }
  .ca-finding__title { font-size: 1rem; font-weight: 700; margin-bottom: 0.4rem; }
  .ca-finding__desc { font-size: 0.9rem; color: #555; line-height: 1.65; }

  .ca-hypothesis {
    background: #1a1a1a;
    color: #fff;
    padding: 2.5rem;
    border-radius: 10px;
  }
  .ca-hypothesis__text {
    font-size: clamp(1rem, 2.5vw, 1.3rem);
    font-weight: 500;
    font-style: italic;
    line-height: 1.55;
    margin-bottom: 1rem;
  }
  .ca-hypothesis__note { font-size: 0.9rem; opacity: 0.6; line-height: 1.6; }

  .ca-hmw {
    background: var(--ca-surface);
    border: 1px solid var(--ca-border-ca);
    border-radius: 10px;
    padding: 1.75rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  .ca-hmw__item {
    font-size: 0.95rem;
    color: #333;
    line-height: 1.6;
    padding-left: 1rem;
    border-left: 2px solid #ccc;
  }

  .ca-timeline {
    display: flex;
    flex-direction: column;
    padding-left: 1.5rem;
    border-left: 1px solid var(--ca-border-ca);
    margin-top: 0.5rem;
  }
  .ca-tl-item {
    display: flex;
    gap: 1.25rem;
    padding-bottom: 2rem;
    margin-left: -1.5rem;
  }
  .ca-tl-marker {
    width: 28px;
    height: 28px;
    border-radius: 50%;
    background: #1a1a1a;
    color: #fff;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.65rem;
    font-weight: 700;
    flex-shrink: 0;
    margin-top: 0.1rem;
  }
  .ca-tl-content h4 { font-size: 1rem; font-weight: 700; margin-bottom: 0.35rem; }
  .ca-tl-content p { font-size: 0.9rem; color: #555; line-height: 1.7; max-width: 58ch; }

  .ca-states {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 1rem;
    margin-bottom: 1.5rem;
  }
  .ca-state {
    border: 1px solid var(--ca-border-ca);
    border-radius: 8px;
    padding: 1.1rem 1.25rem;
    background: var(--ca-surface);
  }
  .ca-state__pill {
    display: inline-block;
    font-size: 0.72rem;
    font-weight: 600;
    padding: 3px 10px;
    border-radius: 999px;
    margin-bottom: 0.6rem;
  }
  .ca-pill--orden      { background: var(--ca-green-bg); color: var(--ca-green); }
  .ca-pill--recibiendo { background: var(--ca-blue-bg); color: #185FA5; }
  .ca-pill--segundo    { background: var(--ca-purple-bg); color: var(--ca-purple); }
  .ca-pill--desierta   { background: var(--ca-amber-bg); color: var(--ca-amber); }
  .ca-pill--cancelada  { background: var(--ca-red-bg); color: var(--ca-red); }
  .ca-pill--cerrada    { background: var(--ca-gray-bg); color: var(--ca-gray); }
  .ca-state__desc { font-size: 0.85rem; color: #555; line-height: 1.65; }

  .ca-callout {
    border-left: 4px solid #1a1a1a;
    padding: 1.1rem 1.5rem;
    background: var(--ca-surface);
    border-radius: 0 8px 8px 0;
    margin-top: 1.5rem;
  }
  .ca-callout p { font-size: 0.9rem; line-height: 1.7; color: #222; }

  .ca-screens {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    align-items: start;
    margin-top: 1.5rem;
  }
  @media (max-width: 640px) { .ca-screens { grid-template-columns: 1fr; } }
  .ca-screen-label {
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--ca-muted);
    font-weight: 600;
    margin-bottom: 0.75rem;
  }

  .ca-phone-wrap { display: flex; justify-content: center; }
  .ca-phone {
    width: 240px;
    background: #fff;
    border-radius: 20px;
    border: 1px solid var(--ca-border-ca);
    overflow: hidden;
    font-size: 0;
  }
  .ca-phone__bar {
    background: var(--ca-blue);
    padding: 10px 14px 8px;
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .ca-phone__bar span { font-size: 12px; font-weight: 500; color: #fff; }
  .ca-phone__search { padding: 8px 10px; border-bottom: 1px solid var(--ca-border-ca); }
  .ca-phone__search input {
    width: 100%;
    font-size: 11px;
    padding: 6px 8px;
    border: 1px solid var(--ca-border-ca);
    border-radius: 6px;
    background: var(--ca-surface);
    color: #333;
  }
  .ca-phone__filters {
    padding: 6px 10px;
    display: flex;
    gap: 5px;
    border-bottom: 1px solid var(--ca-border-ca);
    overflow: hidden;
  }
  .ca-pf {
    font-size: 10px;
    padding: 3px 8px;
    border: 1px solid var(--ca-border-ca);
    border-radius: 999px;
    color: #555;
    background: transparent;
    cursor: pointer;
    white-space: nowrap;
  }
  .ca-pf--active { background: var(--ca-blue); color: #fff; border-color: var(--ca-blue); }
  .ca-phone__count { padding: 6px 10px; font-size: 10px; color: var(--ca-muted); }
  .ca-phone__card {
    margin: 0 10px 8px;
    border: 1px solid var(--ca-border-ca);
    border-radius: 6px;
    padding: 9px 10px;
    background: #fff;
  }
  .ca-pc-status { font-size: 10px; font-weight: 600; margin-bottom: 4px; }
  .ca-pc-status--orden      { color: var(--ca-green); }
  .ca-pc-status--recibiendo { color: #185FA5; }
  .ca-pc-status--desierta   { color: var(--ca-amber); }
  .ca-pc-status--cancelada  { color: var(--ca-red); }
  .ca-pc-title { font-size: 11px; font-weight: 500; color: #222; line-height: 1.35; margin-bottom: 4px; }
  .ca-pc-org { font-size: 10px; color: var(--ca-muted); margin-bottom: 5px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
  .ca-pc-meta { display: flex; justify-content: space-between; font-size: 10px; color: var(--ca-muted); }
  .ca-pc-meta span:last-child { font-weight: 600; color: #222; }
  .ca-pc-cta { font-size: 10px; color: var(--ca-blue); text-align: right; margin-top: 4px; }
  .ca-phone__pagination {
    padding: 8px 10px;
    display: flex;
    justify-content: center;
    gap: 4px;
    border-top: 1px solid var(--ca-border-ca);
  }
  .ca-ppag {
    width: 20px; height: 20px;
    border-radius: 4px;
    font-size: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--ca-muted);
    border: 1px solid var(--ca-border-ca);
    cursor: pointer;
  }
  .ca-ppag--active { background: var(--ca-blue); color: #fff; border-color: var(--ca-blue); }

  .ca-comp-block { margin-bottom: 1.5rem; }
  .ca-comp-block__label {
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--ca-muted);
    font-weight: 600;
    margin-bottom: 0.75rem;
  }
  .ca-pills {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    padding: 1.25rem;
    background: var(--ca-surface);
    border: 1px solid var(--ca-border-ca);
    border-radius: 8px;
  }
  .ca-filter-demo {
    background: #fff;
    border: 1px solid var(--ca-border-ca);
    border-radius: 8px;
    padding: 1rem 1.25rem;
  }
  .ca-filter-row {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 0.5rem 0;
    border-bottom: 1px solid var(--ca-border-ca);
  }
  .ca-filter-row:last-child { border-bottom: none; }
  .ca-filter-label { font-size: 0.78rem; color: var(--ca-muted); width: 90px; flex-shrink: 0; }
  .ca-filter-tag { font-size: 0.75rem; background: var(--ca-blue-bg); color: #185FA5; padding: 2px 10px; border-radius: 999px; font-weight: 500; }
  .ca-filter-val { font-size: 0.82rem; color: #333; }

  .ca-outcomes {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 1rem;
    margin-top: 0.5rem;
  }
  .ca-outcome { border-top: 2px solid #1a1a1a; padding-top: 0.75rem; }
  .ca-outcome__num { display: block; font-size: 1.5rem; font-weight: 700; margin-bottom: 0.4rem; }
  .ca-outcome p { font-size: 0.85rem; color: #555; line-height: 1.6; }

  @media (max-width: 480px) {
    .ca-metrics { grid-template-columns: 1fr 1fr; }
    .ca-states { grid-template-columns: 1fr; }
    .ca-outcomes { grid-template-columns: 1fr 1fr; }
  }

  @media (prefers-color-scheme: dark) {
    .ca-hero__sub, .ca-prose p, .ca-tl-content p,
    .ca-finding__desc, .ca-state__desc, .ca-outcome p { color: #aaa; }
    .ca-hypothesis { background: #f0efe9; color: #111; }
    .ca-phone { background: #1a1a18; border-color: #2e2e2c; }
    .ca-phone__search input { background: #222220; border-color: #2e2e2c; color: #ccc; }
    .ca-phone__card { background: #1a1a18; border-color: #2e2e2c; }
    .ca-pc-title { color: #ddd; }
    .ca-filter-demo { background: #1a1a18; }
    .ca-callout { background: #1a1a18; }
    .ca-callout p { color: #ccc; }
    .ca-hmw__item { color: #bbb; }
    .ca-metric { background: #1a1a18; }
  }
</style>

